info:
  name: getAllShelfs
  type: http

http:
  method: GET
  url: http://localhost:8080/shelf-manager/get-overview/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
