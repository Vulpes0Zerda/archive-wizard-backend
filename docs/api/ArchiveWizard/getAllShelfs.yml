info:
  name: getAllShelfs
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/shelf-manager/get-overview/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
