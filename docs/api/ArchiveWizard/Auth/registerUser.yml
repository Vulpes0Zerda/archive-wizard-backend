info:
  name: registerUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth-manager/register
  body:
    type: json
    data: |-
      {
        "name":"Frieren",
        "email":"strongest.elvemage@post.hr",
        "password":"1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
